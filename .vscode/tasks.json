
{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Profile seasonality.py",
            "type": "shell",
            "command": "source .venv/bin/activate && python -m cProfile -o seasonality.prof seasonality.py && snakeviz seasonality.prof"
            }
        }
    ]
}
